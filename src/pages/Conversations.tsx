import { Paper, Typography } from "@mui/material"

function Conversations() {

  return (
    <Paper elevation={2} sx={{padding: '1.5rem'}}>
      <Typography variant="h4" sx={{ mb:'2rem' }}>Conversations</Typography>
      <Typography>
        Work in progress.
      </Typography>
    </Paper>
  )
}

export default Conversations