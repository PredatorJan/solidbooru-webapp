import { Box, Paper, Typography } from "@mui/material"

function Images() {
  return (
    <Paper elevation={2} sx={{ padding: '1.5rem'}}>
      <Typography variant="h4" sx={{ mb:'2rem' }}>Images</Typography>
      <Typography>
        Work in progress.
      </Typography>
    </Paper>
  )
}

export default Images